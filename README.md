# Clone the repository
	cd ~/git
	git clone https://github.com/sugizo/roots-build

# Create File
	cd roots

# Add, commit, and push your changes
	git add --all
	git commit -m "Initial commit"
	git push -u origin master
